# キミイロ TikTok成長ダッシュボード セットアップガイド

## システム構成（完全無料）

```
┌──────────────────────────┐     ┌──────────────────────────┐
│  GitHub Pages            │────→│  Google スプレッドシート     │
│  フロントエンド（React）    │ API │  データベース代わり          │
│  無料                     │←────│  Google Apps Script       │
│                          │     │  無料                     │
└──────────────────────────┘     └──────────────────────────┘
```

**サーバー費用: 0円**

---

## Step 1: Google スプレッドシートの準備

1. [Google スプレッドシート](https://sheets.google.com) を新規作成
2. シート名を以下の2つに変更:
   - `Characters` （キャラクター情報）
   - `Records` （フォロワー記録）

※ ヘッダー行は自動で作成されるので空のままでOK

## Step 2: Google Apps Script のデプロイ

1. スプレッドシートで **拡張機能** → **Apps Script** を開く
2. `Code.gs` の中身を全て削除し、`gas/Code.gs` の内容を貼り付け
3. **デプロイ** → **新しいデプロイ** をクリック
4. 設定:
   - **種類**: ウェブアプリ
   - **実行ユーザー**: 自分
   - **アクセス**: 全員
5. **デプロイ** をクリック
6. 表示される URL をコピー（後で使います）

> URL形式: `https://script.google.com/macros/s/xxxxx/exec`

## Step 3: GitHub Pages の設定

1. GitHubリポジトリの **Settings** → **Pages** を開く
2. Source を **GitHub Actions** に設定
3. `main` ブランチにマージすると自動デプロイされる

## Step 4: ダッシュボードの初期設定

1. GitHub Pages の URL にアクセス
   - `https://hanour-design.github.io/kimiiro-tiktok-/`
2. 自動で **設定画面** に遷移
3. Step 2 でコピーした GAS の URL を貼り付け
4. **接続テスト** をクリックして確認
5. **サンプルデータ投入** をクリック（任意）

---

## 日常の使い方

### フォロワー数を記録する

1. ダッシュボード → **記録入力**
2. キャラクターを選択
3. 今日のフォロワー数を入力
4. **記録する** をクリック

### データをスプレッドシートで直接確認

Google スプレッドシートを開けば、全データをスプレッドシート上でも確認・編集できます。

### CSV出力

ダッシュボード → **CSV出力** ボタンでダウンロード

---

## GAS を更新した場合

Apps Script のコードを変更した場合は、再デプロイが必要:

1. Apps Script エディタで **デプロイ** → **デプロイを管理**
2. 右上の鉛筆アイコン → **バージョン: 新しいバージョン**
3. **デプロイ** をクリック

---

## トラブルシューティング

### 「接続失敗」が出る
- GAS のデプロイ URL が正しいか確認
- GAS のアクセス設定が「全員」になっているか確認
- 初回アクセス時にGoogleの認証ダイアログが出る場合がある

### データが表示されない
- スプレッドシートのシート名が `Characters` と `Records` になっているか確認
- GAS を再デプロイしてURLを更新
